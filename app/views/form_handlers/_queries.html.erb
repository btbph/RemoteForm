<% if (Message.count % 10 == 0) or (Message.count % 10 > 4)%>
    Запросов
<% end %>
<% if Message.count % 10 == 1 %>
    Запрос
<% end %>
<% if (Message.count % 10 == 2) or (Message.count % 10 == 3) or (Message.count % 10 == 4)%>
    Запроса
<% end %>